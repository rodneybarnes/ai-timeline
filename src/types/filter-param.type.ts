/**
 * The values we need to apply a filter to our entries
 * @property {string} by The category to filter by.
 * @property {string} value The value to filter for the given category.
 */
export type FilterParam = {
    by: string,
    value: string
}